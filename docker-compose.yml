services:
  mantiqh-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mantiqh-web
    ports:
      - "3002:3002"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=3002
    restart: unless-stopped
